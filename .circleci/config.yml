version: 2.1


parameters:
  full_plan_apply: 
    type: string
    default: ""

orbs:
  aws-cli: circleci/aws-cli@3.1.4
jobs:
  aws-cli-cred-setup: 
    executor: aws-cli/default
    steps: 
      - aws-cli/setup:
          aws-access-key-id: AWS_ACCESS_KEY_ID
          aws-secret-access-key: AWS_SECRET_ACCESS_KEY
          aws-region: AWS_REGION
  init-and-plan-test:
    docker: 
      - image: docker.mirror.hashicorp.services/hashicorp/terraform:light
    steps: 
      - checkout
      - run: 
          name: terraform init and plan
          command: | 
            cd infrastructure
            terraform init -input=false -auto-approve -var-file=test.tfvars
            terraform plan
  apply-test:
    docker: 
      - image: docker.mirror.hashicorp.services/hashicorp/terraform:light
    steps: 
      - checkout
      - run: 
          name: terraform manually do full apply
          command: | 
            cd infrastructure
            terraform init -input=false
            terraform apply -input=false -auto-approve -var-file=test.tfvars 
workflows: 
  on-merge:
    jobs: 
      - aws-cli-cred-setup:
          filters: 
            branches: 
              only: main
      - init-and-plan-test:
          requires:
            - aws-cli-cred-setup
          filters: 
            branches: 
              only: main